#include <stdio.h>
#include <unistd.h>

/**
 * main - entry point
 * print the alphabet in lowercase
 *
 * Return: always return 0 to main.
*/
int main(void)
{
	int i;

	for (i = 'a'; i <= 'z' i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
